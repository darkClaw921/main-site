[project]
name = "main-site"
version = "0.1.0"
description = "Web application with real-time clock display"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "loguru>=0.7.2",
    "jinja2>=3.1.4",
]
